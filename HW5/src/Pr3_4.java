/*Write a program to detected and print prime numbers
(integers evenly divisible only by themselves and 1),
using two nested for loops and the modulus operator (%).
 */
public class Pr3_4 {
    public static void main(String[] args) {
        Random rnd = new Random();

        rnd.i = 5;
        rnd.j = 5;

        int [][] arr = rnd.createArray();
        rnd.showArray(arr);
        //int[][] arrchange = rnd.changeArray();
        //rnd.showArray(arrchange);


    }

}
